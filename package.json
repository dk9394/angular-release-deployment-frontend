{
  "name": "angular-release-deployment-frontend",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test",
    "lint": "ng lint",
    "format": "prettier --write \"src/**/*.{ts,html,scss,css,json}\"",
    "format:check": "prettier --check \"src/**/*.{ts,html,scss,css,json}\"",
    "lint:fix": "ng lint --fix",
    "prepare": "husky",
    "//": "─────────────────────────────────────────────────────────────────────",
    "// Deployment Scripts": "For local emergency deployments only",
    "// NOTE": "Production deployments should use GitHub Actions (Phase 5)",
    "//  ": "─────────────────────────────────────────────────────────────────────",
    "deploy": "npm run deploy:dev",
    "deploy:help": "echo '\n📦 Available deployment commands:\n\n  Bash scripts (Mac/Linux):\n    npm run deploy:dev      → Deploy to development\n    npm run deploy:qa       → Deploy to QA\n    npm run deploy:staging  → Deploy to staging\n    npm run deploy:prod     → Deploy to production\n\n  Node.js scripts (Cross-platform):\n    npm run deploy:dev:node      → Deploy to development\n    npm run deploy:qa:node       → Deploy to QA\n    npm run deploy:staging:node  → Deploy to staging\n    npm run deploy:prod:node     → Deploy to production\n\n  ⚠️  Use these only for emergency manual deployments!\n  ✅ Production deployments should use GitHub Actions\n'",
    "deploy:dev": "./deploy.sh dev",
    "deploy:qa": "./deploy.sh qa",
    "deploy:staging": "./deploy.sh staging",
    "deploy:prod": "./deploy.sh prod",
    "deploy:dev:node": "node deploy.mjs dev",
    "deploy:qa:node": "node deploy.mjs qa",
    "deploy:staging:node": "node deploy.mjs staging",
    "deploy:prod:node": "node deploy.mjs prod"
  },
  "prettier": {
    "printWidth": 100,
    "singleQuote": true,
    "overrides": [
      {
        "files": "*.html",
        "options": {
          "parser": "angular"
        }
      }
    ]
  },
  "private": true,
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "@angular/common": "^21.0.0",
    "@angular/compiler": "^21.0.0",
    "@angular/core": "^21.0.0",
    "@angular/forms": "^21.0.0",
    "@angular/platform-browser": "^21.0.0",
    "@angular/router": "^21.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^21.0.4",
    "@angular/cli": "^21.0.4",
    "@angular/compiler-cli": "^21.0.0",
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "angular-eslint": "21.1.0",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.4",
    "husky": "^9.1.7",
    "jsdom": "^27.1.0",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "typescript": "~5.9.2",
    "typescript-eslint": "8.47.0",
    "vitest": "^4.0.8"
  }
}
